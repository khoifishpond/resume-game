import Phaser from 'phaser'

export class MainGame extends Phaser.Scene {
  constructor() {
    super('MainGame')
  }

  create() {
    // World will be built out here — tilemaps, player, markers, etc.
  }

  update() {
    // Per-frame game logic goes here
  }
}
